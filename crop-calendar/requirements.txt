Flask==3.0.3
Werkzeug==3.0.3

# Production WSGI server (used when deployed - see render.yaml/DEPLOYMENT.md).
# python app.py still works fine for local dev; this is only for hosting.
gunicorn==23.0.0

# Required by db.py to connect to PostgreSQL, and to load DB credentials
# from .env (see PGADMIN_SETUP.md).
psycopg2-binary==2.9.12
python-dotenv==1.2.3

# Required by models/weather_utils.py for live Open-Meteo weather calls.
requests==2.32.3

# Required by models/ml_model.py to load the trained XGBoost yield model,
# its encoders, and the real historical climate/agriculture dataset.
scikit-learn==1.9.0
pandas==2.3.3
numpy==2.3.4
xgboost==3.3.0
joblib==1.5.3
